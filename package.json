{
  "name": "image-translator-extension",
  "version": "0.3.0",
  "private": true,
  "description": "Chrome extension that recognizes and translates text inside images.",
  "scripts": {
    "build": "node scripts/build.mjs"
  },
  "dependencies": {
    "tesseract.js": "^6.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
